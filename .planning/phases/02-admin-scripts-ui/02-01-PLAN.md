---
phase: 02-admin-scripts-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/(admin)/admin/scripts/page.tsx]
autonomous: true

must_haves:
  truths:
    - "Admin can view list of all agent scripts"
    - "Scripts display name, business type, interest level, status"
    - "Admin can filter by business type and interest level"
    - "Admin can toggle script active status"
  artifacts:
    - path: "app/(admin)/admin/scripts/page.tsx"
      provides: "Scripts list page with DataTable"
      min_lines: 200
---

<objective>
Build admin scripts list page with DataTable, filters, and CRUD actions.

Purpose: Enable admins to view and manage AI agent scripts.
Output: /admin/scripts page with full listing and actions.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Create scripts list page</name>
  <files>app/(admin)/admin/scripts/page.tsx</files>
  <action>
  Create admin scripts page following commission-tiers pattern:
  - DataTable with columns: name, businessType, interestLevel, isActive, priority, actions
  - Filters for businessType and interestLevel
  - Create/Edit/Delete actions
  - Modal for create/edit form
  </action>
  <verify>Page renders without errors</verify>
  <done>Scripts list page functional</done>
</task>
</tasks>
